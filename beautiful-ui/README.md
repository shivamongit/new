# Beautiful UI Replica

Replica of [Beautiful UI](https://beautiful-ui-five.vercel.app) — crafted primitives for AI-native interfaces.

## Development

```bash
pnpm install
pnpm dev
```

Open http://localhost:3001

## Build

```bash
pnpm build
```

Static export output is in `out/`.
