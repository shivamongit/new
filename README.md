# new

Internal monorepo workspace.

## LoopForge

Windows POC deployment platform — see [loopforge/README.md](loopforge/README.md).

```bash
cd loopforge && pnpm install && pnpm db:push && pnpm dev
```
